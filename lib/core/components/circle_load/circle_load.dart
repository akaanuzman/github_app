import 'package:flutter/material.dart';

class CircleLoad extends Center {
  const CircleLoad({Key? key})
      : super(
          key: key,
          child: const CircularProgressIndicator(),
        );
}
